# Makefile.am for vktor library

VKTOR_LDFLAGS = -module -avoid-version

pkglib_LTLIBRARIES = vktor.la

vktor_la_SOURCES = vktor.c \
                   vktor.h

vktor_la_LDFLAGS = $(VKTOR_LDFLAGS)

AM_CFLAGS = $(DEPOS_CFLAGS)
